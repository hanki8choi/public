aa = []  리스트 선언
aa,bb,cc = [None]*6

리스트 함수
append(): 뒤에 추가 
pop(): 뒤에 뺀다.
insert(위치,값) : 위치에 값을 추가
index(값) : 값을찾아서 위치를 리턴
remove(값) : 첫번째로 찾은 값을 지운다.
extend(리스트) : 리스트 추가

count(값) : 값의 개수를 센다.

len() : 항목 개수
clear() : 모두지움
del(위치) : 해당 위치를 지운다.
sorted() : 소팅해서 list를 리턴함.



for i in range(0,101) :
for i in range[0,1..100] :

aa[0:3]  --> 0~2까지
aa[2:4]  --> 2~3까지

리스트[] 튜플() 릭셔너리 {Key1:Val1, Key2:Val2 ... }

